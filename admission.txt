 using System;
using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.IO;

public class AddimissionProcess{
	public float ScorePer {get; set;}
	public int ConductRating {get;set;}
	public AddimissionProcess(float score, int rate){
		ScorePer=score;
		ConductRating=rate;
	}
}
public class AddmissionRequirement{
	float minscoreperc {get; set;}
	int minconductrating {get; set;}
	public AddmissionRequirement(float per,int rating){
		minscoreperc=per;
		minconductrating=rating;
	}
}
public class Admit{
	public static void Main(){
		AddimissionProcess d=new AddimissionProcess(70.0f,7);
		AddmissionRequirement a=new AddmissionRequirement(60.0f,3);
		try{
			if(CheckScore(d.ScorePer)&&CheckRating(d.ConductRating)){
				Console.WriteLine(" your Addmission was  Granted");
				
			}
			else
			{
				throw new AplogoyException("You aren't eligible ");
			}
		}
		catch (AplogoyException e){
			Console.WriteLine(e.Message);
		}
	}
	class AplogoyException: Exception{
		public AplogoyException(string message):base(message){
		}
	}
	public static bool CheckScore(float scr){
		bool b;
		if(scr>60){
			b=true;
		}
		else
		b=false;
		return b;
	}
	public static bool CheckRating(int rate){
		bool b;
		if(rate>3){
			b=true;
		}
		else
		b=false;
		return b;
	}
}